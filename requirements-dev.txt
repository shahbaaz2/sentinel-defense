-r requirements.txt

types-PyYAML>=6.0
pytest>=8.3
pytest-asyncio>=0.24
ruff>=0.7
mypy>=1.13

# Phase 5: local AI Analyst runtime (Apple Silicon only). Never a cloud LLM SDK. Not installed in
# production (requirements.txt) - a Render/Linux deployment uses SENTINEL_LLM_PROVIDER=deepseek
# instead (ai/providers/deepseek_provider.py). See docs/deployment.md.
mlx>=0.18
mlx-lm>=0.19
