# Production runtime dependencies only - what Render (or any non-Apple-Silicon host) actually
# needs to run apps/api, apps/missionnet, and apps/demo_control. Deliberately excludes mlx/mlx-lm
# (Apple-Silicon-only wheels that don't exist for Linux and aren't needed when
# SENTINEL_LLM_PROVIDER=deepseek - see ai/providers/deepseek_provider.py) and every dev-only tool
# (pytest, ruff, mypy) in requirements-dev.txt. Keep this list in sync with requirements-dev.txt's
# non-mlx, non-dev entries - see docs/deployment.md.
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
sqlalchemy[asyncio]>=2.0
asyncpg>=0.30
alembic>=1.14
httpx>=0.27
pyyaml>=6.0
